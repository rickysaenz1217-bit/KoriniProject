import { S } from '../../styles/StLoading';

const Loading = () => {
  return (
    <>
      <S.LoadingBackground>
        <S.LoadingAnimation />
      </S.LoadingBackground>
    </>
  );
};

export default Loading;
